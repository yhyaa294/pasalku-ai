{
  "buildCommand": "npm run build",
  "outputDirectory": "out",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "http://localhost:8000/api/$1"
    }
  ],
  "env": {
    "ENVIRONMENT": "development",
    "DEBUG": "true"
  }
}